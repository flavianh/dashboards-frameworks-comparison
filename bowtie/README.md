# Failed attempt at running the main example of bowtie

You'll have to install webpack and yarn globally: `npm install -g webpack yarn`

You can try to build the app running `python example.py build`.